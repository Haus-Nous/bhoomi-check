"use client";
import { useEffect, useState } from "react";
import { CasePage } from "@/components/case-page";
import { useCaseActions } from "@/components/case-context";
import { CaseHeader, CaseNavigation } from "@/components/shell";
import { DocumentCard, EmptyState } from "@/components/domain";
import type { DocumentExtraction, SyntheticDocumentFixture } from "@/types/case";
import { useTranslation } from "@/components/locale-context";

export default function DocumentsPage() { const c = useTranslation(); const [selected, setSelected] = useState<string | null>(null); const [fixtures, setFixtures] = useState<SyntheticDocumentFixture[]>([]); const [extraction, setExtraction] = useState<DocumentExtraction | null>(null); const [extracting, setExtracting] = useState(false); const [error, setError] = useState(""); const { listFixtures, attachFixture } = useCaseActions(); useEffect(() => { void listFixtures().then(setFixtures); }, [listFixtures]); const runExtraction = async (caseId: string, documentId: string) => { setExtracting(true); setError(""); try { const response = await fetch(`/api/cases/${encodeURIComponent(caseId)}/documents/${encodeURIComponent(documentId)}/extract`, { method: "POST" }); const body = await response.json() as { data?: DocumentExtraction }; if (!response.ok || !body.data) throw new Error(); setExtraction(body.data); } catch { setError(c.documents.extractionFailure); } finally { setExtracting(false); } }; return <CasePage>{(detail, caseId) => { const item = detail.documents.find((document) => document.id === selected); return <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title={c.documents.header} subtitle={c.documents.subtitle} /><section className="fixture-panel"><p className="eyebrow">{c.documents.add}</p><h2>{c.documents.choose}</h2><div className="fixture-list">{fixtures.map((fixture) => <button key={fixture.id} type="button" className="button secondary" disabled={detail.documents.some((document) => document.kind === fixture.kind)} onClick={() => void attachFixture(caseId, fixture.id)}>{fixture.title}</button>)}</div><p className="micro">{c.documents.fixturesNotice}</p></section><div className="document-list">{detail.documents.length ? detail.documents.map((document) => <article key={document.id} className="document-workflow"><button type="button" className="document-select" style={{ display: "block", width: "100%", padding: 0, border: 0, background: "transparent", textAlign: "left" }} aria-expanded={selected === document.id} aria-controls={selected === document.id ? "extracted-fields" : undefined} onClick={() => { setSelected(document.id); setExtraction(null); setError(""); }}><DocumentCard document={document} /></button></article>) : <EmptyState title={c.documents.noDocuments} detail={c.documents.noDocumentsDetail} />}</div>{item && <section id="extracted-fields" className="inspect" aria-live="polite"><div className="section-head"><div><p className="eyebrow">{c.documents.source}</p><h2>{item.title}</h2></div><button className="button secondary" onClick={() => setSelected(null)}>{c.documents.close}</button></div><pre className="source-text">{item.sourceText}</pre><div className="section-head"><div><p className="eyebrow">{c.documents.extraction}</p><h2>{c.documents.suggested}</h2></div><button className="button small" disabled={extracting} onClick={() => void runExtraction(caseId, item.id)}>{extracting ? c.documents.extracting : c.documents.runExtraction}</button></div><p className="micro">{c.documents.extractionNotice}</p>{error && <p className="form-error" role="alert">{error}</p>}{extraction?.result?.facts.map((fact, index) => <div className="field-row" key={`${fact.key}-${index}`}><span>{fact.key}</span><strong>{fact.value}</strong><span><span className="confidence">{fact.confidence} {c.documents.extractionConfidence}</span><br />{c.labels.evidence}: “{fact.evidence.quote}”{fact.uncertainty ? ` · ${fact.uncertainty}` : ""}</span></div>)}</section>}</div></main>; }}</CasePage>; }
