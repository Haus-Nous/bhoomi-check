"use client";
import { useEffect, useState } from "react";
import { CasePage } from "@/components/case-page";
import { useCaseActions } from "@/components/case-context";
import { CaseHeader, CaseNavigation } from "@/components/shell";
import { DocumentCard, EmptyState } from "@/components/domain";
import type { DocumentExtraction, SyntheticDocumentFixture } from "@/types/case";

export default function DocumentsPage() { const [selected, setSelected] = useState<string | null>(null); const [fixtures, setFixtures] = useState<SyntheticDocumentFixture[]>([]); const [extraction, setExtraction] = useState<DocumentExtraction | null>(null); const [extracting, setExtracting] = useState(false); const [error, setError] = useState(""); const { listFixtures, attachFixture } = useCaseActions(); useEffect(() => { void listFixtures().then(setFixtures); }, [listFixtures]); const runExtraction = async (caseId: string, documentId: string) => { setExtracting(true); setError(""); try { const response = await fetch(`/api/cases/${encodeURIComponent(caseId)}/documents/${encodeURIComponent(documentId)}/extract`, { method: "POST" }); const body = await response.json() as { data?: DocumentExtraction; error?: { message: string } }; if (!response.ok || !body.data) throw new Error(body.error?.message || "AI extraction failed."); setExtraction(body.data); } catch (reason) { setError(reason instanceof Error ? reason.message : "AI extraction failed."); } finally { setExtracting(false); } }; return <CasePage>{(detail, caseId) => { const item = detail.documents.find((document) => document.id === selected); return <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title="Your documents" subtitle="Use only the bundled synthetic fixtures in this prototype." /><section className="fixture-panel"><p className="eyebrow">Add a synthetic document</p><h2>Choose a demo record to add</h2><div className="fixture-list">{fixtures.map((fixture) => <button key={fixture.id} type="button" className="button secondary" disabled={detail.documents.some((document) => document.kind === fixture.kind)} onClick={() => void attachFixture(caseId, fixture.id)}>{fixture.title}</button>)}</div><p className="micro">Synthetic fixtures only. No real documents are accepted or sent anywhere.</p></section><div className="document-list">{detail.documents.length ? detail.documents.map((document) => <article key={document.id} className="document-workflow"><button type="button" className="document-select" style={{ display: "block", width: "100%", padding: 0, border: 0, background: "transparent", textAlign: "left" }} aria-expanded={selected === document.id} aria-controls={selected === document.id ? "extracted-fields" : undefined} onClick={() => { setSelected(document.id); setExtraction(null); setError(""); }}><DocumentCard document={document} /></button></article>) : <EmptyState title="No documents added" detail="Choose a synthetic fixture above to start." />}</div>{item && <section id="extracted-fields" className="inspect" aria-live="polite"><div className="section-head"><div><p className="eyebrow">Synthetic source document</p><h2>{item.title}</h2></div><button className="button secondary" onClick={() => setSelected(null)}>Close</button></div><pre className="source-text">{item.sourceText}</pre><div className="section-head"><div><p className="eyebrow">AI-assisted extraction</p><h2>Suggested fields</h2></div><button className="button small" disabled={extracting} onClick={() => void runExtraction(caseId, item.id)}>{extracting ? "Extracting…" : "Run AI extraction"}</button></div><p className="micro">Informational suggestions only. Confidence describes extraction clarity, not ownership, legal validity, or government verification. Check every value against its quoted source evidence.</p>{error && <p className="form-error" role="alert">{error}</p>}{extraction?.result?.facts.map((fact, index) => <div className="field-row" key={`${fact.key}-${index}`}><span>{fact.key}</span><strong>{fact.value}</strong><span><span className="confidence">{fact.confidence} extraction confidence</span><br />Evidence: “{fact.evidence.quote}”{fact.uncertainty ? ` · ${fact.uncertainty}` : ""}</span></div>)}</section>}</div></main>; }}</CasePage>; }
