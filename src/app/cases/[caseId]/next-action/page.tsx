"use client";
import { useState } from "react";
import { CasePage } from "@/components/case-page";
import { CaseHeader, CaseNavigation } from "@/components/shell";
import { EmptyState } from "@/components/domain";
export default function NextActionPage() { const [chosen, setChosen] = useState<string | null>(null); return <CasePage>{(detail, caseId) => { const action = detail.nextAction; if (!action) return <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title="Choose your next step" subtitle="This guide helps you prepare. It never sends anything or makes a legal decision." /><EmptyState title="No recommendation yet" detail="Add synthetic records before a next step can be suggested." /></div></main>; return <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title="Choose your next step" subtitle="This guide helps you prepare. It never sends anything or makes a legal decision." /><section className="guidance"><p className="eyebrow">Recommended for this case</p><h2>{action.recommendedTitle}</h2><p>{action.recommendedDetail}</p><ol>{action.steps.map((step) => <li key={step}>{step}</li>)}</ol></section><section><p className="eyebrow">Other safe ways to continue</p><div className="action-grid selectable">{action.options.map((option) => <button key={option.id} type="button" className={`action-card ${chosen === option.id ? "selected" : ""}`} aria-pressed={chosen === option.id} onClick={() => setChosen(option.id)}><div><h3>{option.title}</h3><p>{option.detail}</p></div><span aria-hidden>{chosen === option.id ? "✓" : "→"}</span></button>)}</div></section><p className="micro">Mock only — no claim, objection, or government application will be submitted from BhoomiCheck.</p></div></main>; }}</CasePage>; }
