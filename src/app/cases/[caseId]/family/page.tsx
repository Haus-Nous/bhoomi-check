"use client";
import { CasePage } from "@/components/case-page";
import { CaseHeader, CaseNavigation } from "@/components/shell";
import { EmptyState, FamilyMemberCard } from "@/components/domain";
export default function FamilyPage() { return <CasePage>{(detail, caseId) => <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title="Family information" subtitle="A simple view of relationships mentioned in your synthetic records." />{detail.family.members.length ? <div className="relationship">{detail.family.members.map((member, index) => <div key={member.id}><FamilyMemberCard {...member} />{index < detail.family.members.length - 1 && <div className="connector">↓<span>{detail.family.relationships[index]?.label || "relationship noted"}</span></div>}</div>)}</div> : <EmptyState title="No family information yet" detail="Add synthetic family information when it is available." />}<aside className="callout"><strong>What to check</strong><p>This is a summary of stated relationships, not a decision about inheritance or ownership. Review the source information before relying on it.</p></aside></div></main>}</CasePage>; }
