"use client";
import { useEffect, useState } from "react";
import { useSearchParams } from "next/navigation";
import { CasePage } from "@/components/case-page";
import { CaseHeader, CaseNavigation } from "@/components/shell";
import type { ReviewPacket } from "@/types/case";
import { useTranslation } from "@/components/locale-context";
import { useLocale } from "@/components/locale-context";
import { localizedPacketPresentation } from "@/lib/i18n";
function PacketContent({ caseId, result }: { caseId: string; result: string | null }) { const c = useTranslation(); const { locale } = useLocale(); const [packet, setPacket] = useState<ReviewPacket | null>(null); const [notes, setNotes] = useState(""); const [request, setRequest] = useState(""); const [message, setMessage] = useState(""); useEffect(() => { if (!result) return; void fetch(`/api/cases/${caseId}/review-packets`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ verificationResultId: result }) }).then(async (response) => { const body = await response.json() as { data?: ReviewPacket }; if (body.data) { setPacket(body.data); setNotes(body.data.citizenNotes); setRequest(body.data.requestedReviewText); } else setMessage(c.packet.failed); }).catch(() => setMessage(c.packet.failed)); }, [caseId, result, c.packet.failed]); const save = async (ready = false) => { if (!packet) return; const response = await fetch(`/api/cases/${caseId}/review-packets/${packet.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ citizenNotes: notes, requestedReviewText: request, ...(ready ? { status: "READY_FOR_REVIEW" } : {}) }) }); const body = await response.json() as { data?: ReviewPacket }; if (body.data) { setPacket(body.data); setMessage(ready ? c.packet.preparedMessage : c.packet.savedMessage); } else setMessage(c.packet.failed); }; const presentation = packet ? localizedPacketPresentation(locale, packet.issueCategory) : null; const displayedRequest = packet && request === packet.requestedReviewText ? presentation?.request || request : request; return <main id="main" className="case-layout"><CaseNavigation caseId={caseId} /><div className="case-content"><CaseHeader title={c.packet.header} subtitle={c.packet.subtitle} />{message && <p className="callout" role="status">{message}</p>}{packet && presentation ? <section className="guidance-item"><p className="eyebrow">{packet.status === "READY_FOR_REVIEW" ? c.packet.prepared : c.packet.draft}</p><h2>{packet.issueCategory.replace(/_/g, " ")}</h2><p>{presentation.summary}</p><p className="micro"><strong>{c.packet.compared}:</strong> {packet.comparedValues.expected || c.labels.notAvailable} / {packet.comparedValues.observed || c.labels.notAvailable}</p><p className="micro"><strong>{c.packet.supporting}:</strong> {packet.supportingDocumentIds.join(", ") || c.labels.notAvailable}</p><label className="packet-field">{c.packet.notes}<textarea value={notes} disabled={packet.status === "READY_FOR_REVIEW"} onChange={(event) => setNotes(event.target.value)} /></label><label className="packet-field">{c.packet.request}<textarea value={displayedRequest} disabled={packet.status === "READY_FOR_REVIEW"} onChange={(event) => setRequest(event.target.value)} /></label>{packet.status === "DRAFT" && <div className="actions"><button className="button secondary" onClick={() => void save()}>{c.cta.save}</button><button className="button" onClick={() => void save(true)}>{c.cta.readyForReview}</button></div>}<aside className="guidance-caution"><strong>{c.packet.safetyTitle}</strong><p>{c.packet.safetyDetail}</p></aside></section> : <p className="state">{result ? c.packet.preparing : c.packet.noPacketDetail}</p>}</div></main>; }
export default function ReviewPacketPage() { const result = useSearchParams().get("result"); return <CasePage>{(_, caseId) => <PacketContent caseId={caseId} result={result} />}</CasePage>; }
